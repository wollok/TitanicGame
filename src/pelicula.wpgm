import personajes.*
import wollok.game.*

program pelicula {

    pelicula.iniciar()
    mar.tormenta()
    iceberg.aparecer()
    iceberg.moverse()
    iceberg.moverse()
    iceberg.moverse()
    
    
    
    
}